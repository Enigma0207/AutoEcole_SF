{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur{% endblock %}

{% block body %}
    <h4>Modifier l'utilisateur</h4>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Mettre à jour</button>
    {{ form_end(form) }}
    <a href="{{ path('app_userliste') }}"><span>&#128412;</span>List</a>
{% endblock %}
